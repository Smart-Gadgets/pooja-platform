apiVersion: apps/v1
kind: Deployment
metadata:
  name: rag-ai-service
  namespace: pooja-platform
  labels:
    app: rag-ai-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rag-ai-service
  template:
    metadata:
      labels:
        app: rag-ai-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: /actuator/prometheus
        prometheus.io/port: "8085"
    spec:
      containers:
        - name: rag-ai-service
          image: pooja-platform/rag-ai-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8085
          envFrom:
            - configMapRef:
                name: pooja-config
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: pooja-secrets
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: pooja-secrets
                  key: JWT_SECRET
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: pooja-secrets
                  key: OPENAI_API_KEY
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 60
            periodSeconds: 15
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: rag-ai-service
  namespace: pooja-platform
spec:
  selector:
    app: rag-ai-service
  ports:
    - port: 8085
      targetPort: 8085

