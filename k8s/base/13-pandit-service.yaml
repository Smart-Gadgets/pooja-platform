apiVersion: apps/v1
kind: Deployment
metadata:
  name: pandit-service
  namespace: pooja-platform
  labels:
    app: pandit-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pandit-service
  template:
    metadata:
      labels:
        app: pandit-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: /actuator/prometheus
        prometheus.io/port: "8083"
    spec:
      containers:
        - name: pandit-service
          image: pooja-platform/pandit-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: pooja-config
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: pooja-secrets
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: pooja-secrets
                  key: JWT_SECRET
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 60
            periodSeconds: 15
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: pandit-service
  namespace: pooja-platform
spec:
  selector:
    app: pandit-service
  ports:
    - port: 8083
      targetPort: 8083

